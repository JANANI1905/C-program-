//with argument passed without return type

#include<stdio.h>
void area(int a)
{
    printf("Enter the sides : ");
    scanf("%d",&a);
    printf("Area of square is : %d",a*a);
}
int main()
{
    int a;
    area(a);
}

**********************************

output

Enter the sides : 2
Area of square is : 4
