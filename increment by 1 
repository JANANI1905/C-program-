#include<stdio.h>
void main()
{
    int num,sum=0;
    printf("Enter the number : ");
    scanf("%d",&num);

    while(num!=0)
    {
      sum = sum + num%10 + 1;
      num = num/10;
    }
    printf("Sum of the given no is %d",sum);
}